{
  "name": "vercel-momo-server",
  "version": "1.0.0",
  "description": "MoMo payment simulation for WinUI3 desktop app",
  "scripts": {
    "dev": "vercel dev",
    "build": "vercel build",
    "start": "vercel dev"
  },
  "dependencies": {
    "uuid": "^9.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
